---
phase: 02-document-upload-ai-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - package.json
  - package-lock.json
  - .env.local.example
  - .gitignore
autonomous: true
requirements: [UPLD-01, UPLD-02, EXTR-01, EXTR-02]
tags: [next-config, dependencies, env-setup]
user_setup:
  - service: anthropic
    why: "Claude Vision PDF extraction per D-22"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/settings/keys"
must_haves:
  truths:
    - "Server Actions accept FormData payloads up to 15 MB (single-file upload with headroom)"
    - "react-dropzone, pdf-lib, p-limit, @anthropic-ai/sdk are installed at pinned versions"
    - "data/uploads/ directory is gitignored"
    - "ANTHROPIC_API_KEY is documented in .env.local.example (placeholder only)"
  artifacts:
    - path: "next.config.ts"
      provides: "serverActions.bodySizeLimit + proxyClientMaxBodySize raised to 15mb"
      contains: "bodySizeLimit"
    - path: "package.json"
      provides: "Pinned new dependencies"
      contains: "react-dropzone"
    - path: ".env.local.example"
      provides: "ANTHROPIC_API_KEY placeholder"
      contains: "ANTHROPIC_API_KEY"
    - path: ".gitignore"
      provides: "data/uploads/ ignore"
      contains: "data/uploads"
  key_links:
    - from: "next.config.ts"
      to: "Server Action FormData uploads"
      via: "experimental.serverActions.bodySizeLimit"
      pattern: "bodySizeLimit.*15mb"
    - from: "next.config.ts"
      to: "Production standalone body gate"
      via: "experimental.proxyClientMaxBodySize"
      pattern: "proxyClientMaxBodySize.*15mb"
---

<objective>
Prepare the project for PDF upload + Claude Vision extraction by (a) raising the Next 16 Server Action body limits so 10 MB PDFs are not rejected, (b) installing the new npm dependencies locked in CONTEXT/RESEARCH, and (c) wiring the ANTHROPIC_API_KEY env placeholder and `data/uploads/` gitignore entry.

Purpose: Every downstream Phase 2 plan assumes these are in place. Doing this in its own Wave 1 plan unblocks Waves 2–6 to parallelize.
Output: Updated `next.config.ts`, updated `package.json` + lockfile, `.env.local.example` entry, `.gitignore` rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-document-upload-ai-extraction/02-CONTEXT.md
@.planning/phases/02-document-upload-ai-extraction/02-RESEARCH.md
@CLAUDE.md
@next.config.ts
@package.json
@.gitignore

<interfaces>
<!-- Existing next.config.ts (before this plan): empty config object -->
```ts
// next.config.ts
import type { NextConfig } from "next";
const nextConfig: NextConfig = {
  /* config options here */
};
export default nextConfig;
```

<!-- package.json current versions (Phase 1): next 16.2.x, react 19.2.x, @anthropic-ai/sdk NOT installed -->
<!-- New dependencies to add with exact pins (RESEARCH §Standard Stack): -->
<!-- - react-dropzone: 15.0.0 -->
<!-- - pdf-lib: 1.17.1 -->
<!-- - p-limit: 7.3.0 -->
<!-- - @anthropic-ai/sdk: 0.88.0 (CLAUDE.md pins 0.88; latest is 0.90 — honor project pin) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise Next 16 Server Action body limits in next.config.ts</name>
  <read_first>
    - next.config.ts
    - .planning/phases/02-document-upload-ai-extraction/02-RESEARCH.md (§ Next 16 Server Action body size limit)
  </read_first>
  <files>next.config.ts</files>
  <action>
Overwrite `next.config.ts` with the following exact contents:

```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    serverActions: {
      // Per RESEARCH §Next 16 Server Action body size limit.
      // One file per Server Action call (Option A, locked): 10 MB raw + FormData overhead + base64 headroom → 15mb.
      bodySizeLimit: "15mb",
    },
    // Next 15.5+ added a second gate that also defaults to 1 MB for standalone/production (VPS deployment target).
    // Must be raised independently of serverActions.bodySizeLimit.
    // CITED: nextjs.org/docs/app/api-reference/config/next-config-js/proxyClientMaxBodySize
    proxyClientMaxBodySize: "15mb",
  },
};

export default nextConfig;
```

Do NOT introduce turbopack flags, images config, or any other keys. This plan is scoped to body limits only.
  </action>
  <verify>
    <automated>node -e "const c = require('./next.config.ts'.replace(/\\.ts$/,'')) || require('./next.config.js'); process.exit(0)" 2>/dev/null; grep -q 'bodySizeLimit.*15mb' next.config.ts && grep -q 'proxyClientMaxBodySize.*15mb' next.config.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v 'next.config.ts' | (! grep -q 'error TS')</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bodySizeLimit' next.config.ts` returns >= 1
    - `grep -c '"15mb"' next.config.ts` returns >= 2
    - `grep -c 'proxyClientMaxBodySize' next.config.ts` returns >= 1
    - `npx tsc --noEmit` passes without errors in next.config.ts
  </acceptance_criteria>
  <done>next.config.ts has both body limits set to 15mb and TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Install Phase 2 npm dependencies at pinned versions</name>
  <read_first>
    - package.json
    - .planning/phases/02-document-upload-ai-extraction/02-RESEARCH.md (§ Standard Stack, § New dependencies to add)
    - CLAUDE.md (@anthropic-ai/sdk pin)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
Install with EXACT versions (no caret, no range) to match the Phase 1 reproducibility pattern (STATE.md: "Pin better-sqlite3@12.9.0, drizzle-orm@0.45.2 at exact versions").

Run this single command:

```bash
npm install --save-exact react-dropzone@15.0.0 pdf-lib@1.17.1 p-limit@7.3.0 @anthropic-ai/sdk@0.88.0
```

Do NOT run `npm install` without `--save-exact` — carets are forbidden. Do NOT upgrade @anthropic-ai/sdk past 0.88.0 — CLAUDE.md pins 0.88. Do NOT add `@types/*` packages (react-dropzone, pdf-lib, p-limit all ship TS types; @anthropic-ai/sdk is native TS).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json').dependencies; const need={'react-dropzone':'15.0.0','pdf-lib':'1.17.1','p-limit':'7.3.0','@anthropic-ai/sdk':'0.88.0'}; for(const k in need){if(p[k]!==need[k]){console.error('miss',k,p[k],'want',need[k]);process.exit(1)}} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains exact strings: `"react-dropzone": "15.0.0"`, `"pdf-lib": "1.17.1"`, `"p-limit": "7.3.0"`, `"@anthropic-ai/sdk": "0.88.0"`
    - No `^` or `~` prefixes on any of the four new dependencies
    - `package-lock.json` exists and is committed
    - `node_modules/react-dropzone/package.json` exists
    - `node_modules/@anthropic-ai/sdk/package.json` exists
  </acceptance_criteria>
  <done>All four dependencies installed at exact pinned versions; lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 3: Add ANTHROPIC_API_KEY placeholder and data/uploads gitignore</name>
  <read_first>
    - .env.local.example (if exists)
    - .gitignore
    - .planning/phases/02-document-upload-ai-extraction/02-CONTEXT.md (D-06, D-22)
  </read_first>
  <files>.env.local.example, .gitignore</files>
  <action>
1. Append to `.env.local.example` (create the file if it does not exist) the following block exactly:

```
# Anthropic Claude API — Phase 2 (D-22)
# Get a key from https://console.anthropic.com/settings/keys
# Never commit the actual value.
ANTHROPIC_API_KEY=

# Optional — FX rate for audit-only cost logging in extraction_log (D-15).
# Default 0.92 EUR/USD. Set explicitly for accurate accounting.
USD_TO_EUR=0.92
```

Do NOT write a real key. Do NOT create `.env.local` (that is operator's responsibility at execute time).

2. Append to `.gitignore` (idempotent — do not add if already present) exactly:

```
# Phase 2 — uploaded PDFs (D-06: data/uploads/{uuid}.pdf, never committed)
data/uploads/
```

Check with `grep -q 'data/uploads/' .gitignore` before appending.

3. Create the `data/uploads/` directory with a `.gitkeep` so the path exists on fresh clones but contents remain gitignored:

```bash
mkdir -p data/uploads
touch data/uploads/.gitkeep
```

Add an explicit negation in .gitignore so `.gitkeep` is tracked:

```
!data/uploads/.gitkeep
```
  </action>
  <verify>
    <automated>grep -q 'ANTHROPIC_API_KEY' .env.local.example && grep -q 'USD_TO_EUR' .env.local.example && grep -q 'data/uploads/' .gitignore && grep -q '!data/uploads/.gitkeep' .gitignore && test -f data/uploads/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `.env.local.example` contains the literal string `ANTHROPIC_API_KEY=` (empty value, no placeholder secret)
    - `.env.local.example` contains `USD_TO_EUR=0.92`
    - `.gitignore` contains `data/uploads/` on its own line
    - `.gitignore` contains `!data/uploads/.gitkeep` on its own line
    - File `data/uploads/.gitkeep` exists
    - `git check-ignore data/uploads/random.pdf` returns exit 0 (file IS ignored)
    - `git check-ignore data/uploads/.gitkeep` returns exit 1 (file is NOT ignored, due to negation)
  </acceptance_criteria>
  <done>Env placeholder, gitignore entries, and data/uploads/ scaffolded.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes.
- `package.json` shows exact-pinned versions for all four new deps.
- `git status` shows only the four modified files + new `.gitkeep` (no stray artifacts).
- `ls data/uploads/` lists only `.gitkeep`.
</verification>

<success_criteria>
- next.config.ts has `bodySizeLimit: "15mb"` AND `proxyClientMaxBodySize: "15mb"`.
- `react-dropzone@15.0.0`, `pdf-lib@1.17.1`, `p-limit@7.3.0`, `@anthropic-ai/sdk@0.88.0` installed as exact versions.
- `.env.local.example` + `.gitignore` updated.
- Downstream plans (02, 03, 04) can assume SDK + pdf-lib + p-limit are importable and that Server Actions accept up to 15 MB payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-upload-ai-extraction/02-01-SUMMARY.md` using the summary template.
</output>
