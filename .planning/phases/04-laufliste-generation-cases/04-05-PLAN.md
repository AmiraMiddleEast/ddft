---
phase: 04-laufliste-generation-cases
plan: 05
type: execute
wave: 5
depends_on: ["04-02", "04-04"]
files_modified:
  - app/(app)/cases/page.tsx
  - app/(app)/cases/new/page.tsx
  - app/(app)/cases/[id]/page.tsx
  - app/(app)/cases/[id]/CaseDetailClient.tsx
  - app/(app)/cases/[id]/AddDocumentsSheet.tsx
  - app/(app)/cases/[id]/RemoveDocumentDialog.tsx
  - app/(app)/cases/[id]/RegenerateDialog.tsx
  - app/(app)/cases/[id]/DocumentsTable.tsx
  - app/(app)/cases/new/CreateCaseForm.tsx
autonomous: true
requirements: [LAFL-01, LAFL-02, LAFL-03, CASE-01, CASE-02, CASE-03]
tags: [ui, cases, shadcn, forms]

must_haves:
  truths:
    - "/cases lists cases owned by the session user, sorted updated_at DESC, empty state when none"
    - "/cases/new renders a form with Name der Person (required), Geburtsdatum (optional), Notizen (optional) and submits createCaseAction"
    - "/cases/[id] shows person header, documents table with add/remove/reorder, Laufliste section with generate + download, and Historie section"
    - "Add-documents sheet lists reviewed + unassigned documents with checkboxes and a '{n} hinzufügen' primary CTA"
    - "Generate Laufliste button is disabled when case has zero documents OR any document is unapproved"
    - "German UI copy matches UI-SPEC verbatim (D-19)"
  artifacts:
    - path: "app/(app)/cases/page.tsx"
      provides: "/cases list server component"
      contains: "listCasesForUser"
    - path: "app/(app)/cases/new/page.tsx"
      provides: "/cases/new page with CreateCaseForm"
    - path: "app/(app)/cases/[id]/page.tsx"
      provides: "/cases/[id] server-rendered detail page"
      contains: "getCaseForUser"
    - path: "app/(app)/cases/[id]/AddDocumentsSheet.tsx"
      provides: "client component for add-docs multi-select sheet"
  key_links:
    - from: "app/(app)/cases/[id]/CaseDetailClient.tsx"
      to: "generateLauflisteAction"
      via: "useTransition + generateLauflisteAction(caseId)"
      pattern: "generateLauflisteAction"
    - from: "app/(app)/cases/[id]/AddDocumentsSheet.tsx"
      to: "addDocumentsToCaseAction"
      via: "form action or useTransition calling the Server Action"
      pattern: "addDocumentsToCaseAction"
    - from: "app/(app)/cases/new/CreateCaseForm.tsx"
      to: "createCaseAction"
      via: "useActionState(createCaseAction, ...) or direct call"
      pattern: "createCaseAction"
---

<objective>
Build the three case UI routes (`/cases`, `/cases/new`, `/cases/[id]`) plus their client components (CreateCaseForm, DocumentsTable, AddDocumentsSheet, RemoveDocumentDialog, RegenerateDialog, CaseDetailClient). All server components enforce ownership before rendering; all mutations call Plan 02/04 Server Actions; all copy is German verbatim from UI-SPEC.

Purpose: Requirements CASE-01 (create), CASE-02 (add multiple docs), CASE-03 (generate PDF button), plus surfaces the LAFL-01/02/03 outputs for download. Plan 06 adds the Route Handler and Historie download buttons.

Output: A fully-working, clickable Phase 4 UI that lets the operator create a case, add reviewed docs, reorder, and generate a Laufliste.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-laufliste-generation-cases/04-CONTEXT.md
@.planning/phases/04-laufliste-generation-cases/04-UI-SPEC.md
@.planning/phases/04-laufliste-generation-cases/04-02-PLAN.md
@.planning/phases/04-laufliste-generation-cases/04-04-PLAN.md
@app/(app)/page.tsx
@app/(app)/documents/[id]/page.tsx
@app/(app)/upload/page.tsx
@components/ui/button.tsx
@components/ui/dialog.tsx
@components/ui/sheet.tsx
@components/ui/checkbox.tsx
@components/ui/textarea.tsx
@components/ui/table.tsx
@components/ui/badge.tsx

<interfaces>
<!-- Server Actions consumed (from Plans 02 + 04) -->
```typescript
// from "@/lib/cases/actions"
createCaseAction({ personName, personBirthdate?, notes? }): Promise<Result<{ caseId }>>
addDocumentsToCaseAction({ caseId, documentIds: string[] }): Promise<Result<{ added: number }>>
removeDocumentFromCaseAction({ caseId, caseDocumentId }): Promise<Result<{}>>
reorderCaseDocumentsAction({ caseId, caseDocumentId, direction: 'up' | 'down' }): Promise<Result<{}>>

// from "@/lib/laufliste/actions"
generateLauflisteAction(caseId: string): Promise<Result<{ lauflisteId }>>

// Queries used server-side in page.tsx files
listCasesForUser(userId, db), getCaseForUser(caseId, userId, db)
listCaseDocuments(caseId, userId, db), listAssignableDocuments(userId, db)
listLauflistenForCase(caseId, userId, db)
```

Result<T> = { ok: true, data: T } | { ok: false, error: string, details? }

<!-- UI-SPEC copy contract (exact German strings) — see .planning/phases/04-laufliste-generation-cases/04-UI-SPEC.md -->
Cases list: heading "Fälle", CTA "Neuen Fall anlegen", columns "Person/Dokumente/Status/Aktualisiert".
Status badges: open → "In Bearbeitung" (warning), ready_for_pdf → "Bereit" (outline), pdf_generated → "Laufliste erstellt" (secondary).
Create: heading "Neuen Fall anlegen", fields Name der Person (placeholder "Vor- und Nachname"), Geburtsdatum (helper "Optional"), Notizen (placeholder "Interne Hinweise (optional)"), CTAs "Fall anlegen" / "Abbrechen", success toast "Fall angelegt.".
Detail: heading is person name, subtext "Erstellt am {dd.MM.yyyy} · {n} Dokumente", sections "Dokumente" / "Laufliste" / "Historie", CTAs "Dokumente hinzufügen" / "Laufliste generieren" / "Erneut generieren" / "Herunterladen".
Blocker banners: "Bitte mindestens ein Dokument hinzufügen." / "Mindestens ein Dokument ist noch nicht geprüft.".
Remove dialog: heading "Dokument aus Fall entfernen?", body "Das Dokument bleibt erhalten und kann einem anderen Fall zugeordnet werden.", confirm "Entfernen" (destructive).
Regenerate dialog: heading "Laufliste erneut erstellen?", body "Eine neue Laufliste wird erzeugt. Die bisherige bleibt im Verlauf verfügbar.", confirm "Erneut generieren" (accent).
Picker sheet: heading "Dokumente hinzufügen", subtext "Nur geprüfte Dokumente, die keinem anderen Fall zugeordnet sind.", empty "Keine Dokumente verfügbar. Bitte zuerst ein Dokument hochladen und prüfen.", CTA "{n} hinzufügen" (disabled when n=0).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /cases list + /cases/new create form</name>
  <files>app/(app)/cases/page.tsx, app/(app)/cases/new/page.tsx, app/(app)/cases/new/CreateCaseForm.tsx</files>
  <action>
1. `app/(app)/cases/page.tsx` (server component):
   - `await auth.api.getSession({ headers: await headers() })` — redirect to `/login` if null (though protected layout already does this — keep defense-in-depth).
   - Call `listCasesForUser(session.user.id, db)`.
   - Layout per UI-SPEC Cases list:
     - Container `max-w-[1080px]`, top offset 32px.
     - Heading row with `h1` "Fälle" + Link-as-button `Neuen Fall anlegen` (→ `/cases/new`) using `buttonVariants()` on Link (Phase 2 Plan 02-05 precedent — Button primitive is base-ui, no asChild).
     - Subtext muted: "Fälle bündeln Dokumente einer Person zu einer gemeinsamen Laufliste."
     - Card wrapping shadcn Table: columns Person / Dokumente / Status / Aktualisiert / (blank). Row action renders Link "Öffnen" → `/cases/${case.id}`.
     - Status badge variants per UI-SPEC table (warning/outline/secondary).
     - Format `updatedAt` as `dd.MM.yyyy HH:mm` via date-fns format.
     - Documents count per case: either add to queries or render "—" if not trivially available (RECOMMENDED: extend `listCasesForUser` in Plan 02 to include documentCount via `(SELECT COUNT(*) FROM case_document WHERE case_id = case.id)` subquery — but if Plan 02 didn't, do a second query here with `db.select({ caseId, count: sql<number>`count(*)`}).from(caseDocument).groupBy(caseDocument.caseId)` and merge by caseId client-side).
   - Empty state: full-width Card with heading "Noch keine Fälle" + body "Legen Sie einen Fall an, um Dokumente zu einer Person zusammenzufassen." + CTA link.
   - Page title via metadata export: `Fälle — Angela`.

2. `app/(app)/cases/new/page.tsx`:
   - Session check → redirect if null.
   - Renders `<CreateCaseForm />` client component inside a max-w-[560px] container with breadcrumb + heading + subtext per UI-SPEC.
   - Metadata title: "Neuen Fall anlegen — Angela".

3. `app/(app)/cases/new/CreateCaseForm.tsx` ("use client"):
   - Controlled form using `useState` per Phase 3 Plan 03-05 precedent (CONTEXT D-17 pattern — small form, no react-hook-form).
   - Fields: `personName` (Input, required, max 200), `personBirthdate` (Input type="date", optional), `notes` (Textarea, optional, max 2000, min-rows=3).
   - Labels + helpers + placeholders from UI-SPEC verbatim.
   - Submit: wrap call to `createCaseAction` in `useTransition`; on `ok: true`, call `toast.success("Fall angelegt.")` from sonner and `router.push(`/cases/${caseId}`)`; on `ok: false` with error 'VALIDATION', render inline errors under each invalid field; on other errors, `toast.error("Fall konnte nicht angelegt werden. Bitte erneut versuchen.")`.
   - Focus first invalid field on validation error by id.
   - CTA row: `Abbrechen` (Link back to `/cases`, ghost variant) + `Fall anlegen` (primary, type="submit", disabled during transition).
   - Inline validation copy per UI-SPEC: "Pflichtfeld." / "Bitte ein gültiges Datum eingeben." / "Eingabe ist zu lang.".

Implements CASE-01 (UI layer), D-03, D-19, D-20.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "cases/(page|new)" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>Routes render without TS errors; list shows cases in updated_at DESC; create form calls createCaseAction and redirects on success; German copy exact.</done>
</task>

<task type="auto">
  <name>Task 2: /cases/[id] detail page (server) + DocumentsTable + CaseDetailClient</name>
  <files>app/(app)/cases/[id]/page.tsx, app/(app)/cases/[id]/CaseDetailClient.tsx, app/(app)/cases/[id]/DocumentsTable.tsx</files>
  <action>
1. `app/(app)/cases/[id]/page.tsx` (server component):
   - `const { id } = await params;` (Next 16 async params).
   - Session check → redirect.
   - `const caseRow = await getCaseForUser(id, session.user.id, db);` — if null, `notFound()` (Next.js).
   - `const docs = await listCaseDocuments(id, session.user.id, db);`
   - `const lauflisten = await listLauflistenForCase(id, session.user.id, db);` (from Plan 04 queries)
   - Compute flags: `hasUnreviewed = docs.some(d => d.reviewStatus !== 'approved')`; `isEmpty = docs.length === 0`; `canGenerate = !hasUnreviewed && !isEmpty`.
   - Render layout per UI-SPEC Case detail:
     - Container max-w-[1080px], top offset 32px, vertical section gap 32px.
     - Breadcrumb: `Übersicht / Fälle / {person_name}` (Links to `/` and `/cases`).
     - Person header Card: h1 {personName} + status badge right-aligned; row 2 muted subtext `Erstellt am {dd.MM.yyyy} · {n} Dokumente`; row 3 2-col list `Geburtsdatum` / `Notizen` (128px label col) — show "— nicht hinterlegt" when null.
     - Blocker banner (amber fill) if `isEmpty` → "Bitte mindestens ein Dokument hinzufügen."; else if `hasUnreviewed` → "Mindestens ein Dokument ist noch nicht geprüft.".
     - Documents card: heading "Dokumente" + helper "Reihenfolge in der Laufliste — mit den Pfeilen anpassen." + `Dokumente hinzufügen` CTA (opens Sheet — handled in Task 3 via client wrapper).
     - `<DocumentsTable docs={docs} caseId={id} />` client component.
     - Laufliste card: heading "Laufliste" + contextual subtext ("Noch keine Laufliste erstellt." OR "Zuletzt erstellt am {dd.MM.yyyy HH:mm} · {n} Dokumente · {filesize}"); CTAs `Laufliste generieren` (or `Erneut generieren` if latest exists) + `Herunterladen` secondary. These CTAs are in `<CaseDetailClient />` so they can call generateLauflisteAction through useTransition.
   - Pass `latestLaufliste = lauflisten[0] ?? null` and `canGenerate` to client wrapper.
   - `fileSize` formatted via a small `formatBytes()` helper (e.g. "42 KB" or "1.2 MB").
   - Metadata title: `{person_name} — Angela`.

2. `app/(app)/cases/[id]/DocumentsTable.tsx` ("use client"):
   - Props: `{ docs: CaseDocRow[]; caseId: string }` where CaseDocRow is the join shape from Plan 02 `listCaseDocuments`.
   - Renders a shadcn Table with columns `# | Dokument | Dokumenttyp | Status | (actions)`.
   - Row actions: Link "Ansehen" → `/documents/${documentId}` (ghost sm), up arrow button (icon-only 32×32, tooltip "Nach oben", disabled when first row), down arrow (tooltip "Nach unten", disabled when last), `Entfernen` (ghost sm, destructive color, opens RemoveDocumentDialog).
   - Arrow buttons wire to `reorderCaseDocumentsAction({ caseId, caseDocumentId, direction })` via useTransition; optimistic local reorder (swap array items) before awaiting the action; rollback on failure with destructive toast.
   - `Entfernen` sets local state `removingCaseDocumentId` → renders `<RemoveDocumentDialog />` from Task 3.
   - Review status badge: approved → "Geprüft" (outline); other → "Noch nicht geprüft" (warning).
   - Empty state replaces table with centered column "Noch keine Dokumente zugeordnet." + CTA opening the AddDocumentsSheet.
   - Live region: `<div role="status" aria-live="polite">` announcing "Position {n} von {total}." after reorder (UI-SPEC Interaction Contract).

3. `app/(app)/cases/[id]/CaseDetailClient.tsx` ("use client"):
   - Props: `{ caseId; canGenerate; hasLatest: boolean; latestLauflisteId: string | null }`.
   - Primary button: if `latestLauflisteId === null` → "Laufliste generieren" (accent); else → "Erneut generieren" (opens RegenerateDialog first — handled Task 3).
   - Disabled when `!canGenerate` with tooltip (use Button `title` attribute for v1 — shadcn tooltip not vendored) "Alle Dokumente müssen geprüft sein." or "Bitte mindestens ein Dokument hinzufügen.".
   - On press (or after dialog confirm): `startTransition(async () => { const r = await generateLauflisteAction(caseId); ... })`. Swap button label to "Laufliste wird erstellt …" + disable + inline spinner.
   - On ok:true → toast "Laufliste erstellt."; router.refresh() to pick up server-rendered fresh Laufliste + Historie.
   - On ok:false → toast.error "Laufliste konnte nicht erzeugt werden. Bitte erneut versuchen.".
   - Download secondary CTA: `<a href={`/api/cases/${caseId}/laufliste/${latestLauflisteId}/download`}>Herunterladen</a>` styled via buttonVariants (Plan 06 implements the route).

Implements CASE-02 (add via sheet later), CASE-03 (generate CTA), D-03, D-05, D-16, D-17 (disable + banner), D-19, D-20, LAFL-03 (download link placeholder; Route Handler in Plan 06).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "cases/\\[id\\]" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>Detail page renders with ownership guard; DocumentsTable reorder + remove interactions wired; CaseDetailClient calls generateLauflisteAction; blockers render correct banner + disable CTA; all German copy exact.</done>
</task>

<task type="auto">
  <name>Task 3: AddDocumentsSheet + RemoveDocumentDialog + RegenerateDialog</name>
  <files>app/(app)/cases/[id]/AddDocumentsSheet.tsx, app/(app)/cases/[id]/RemoveDocumentDialog.tsx, app/(app)/cases/[id]/RegenerateDialog.tsx</files>
  <action>
1. `AddDocumentsSheet.tsx` ("use client"):
   - Props: `{ caseId: string; onOpenChange?: (open: boolean) => void; assignableDocs: AssignableDocRow[] }`.
   - Assignable docs fetched server-side by `page.tsx` via `listAssignableDocuments` and passed down (RECOMMENDED). Alternative: a Server Action `listAssignableDocumentsAction` invoked on sheet open — prefer server-rendered for simplicity.
   - Wraps shadcn `<Sheet>` component on right side, width 480px on md+.
   - Heading "Dokumente hinzufügen" + subtext "Nur geprüfte Dokumente, die keinem anderen Fall zugeordnet sind.".
   - Scrollable Table with columns (checkbox) / "Dokument" / "Dokumenttyp" / "Hochgeladen". Row height 56px.
   - `const [selected, setSelected] = useState<Set<string>>(new Set())`. Each Checkbox (shadcn) toggles the docId in the Set.
   - Empty state: "Keine Dokumente verfügbar. Bitte zuerst ein Dokument hochladen und prüfen." + Link CTA "Dokument hochladen" → `/upload`.
   - Sticky footer inside sheet: primary "{n} hinzufügen" (disabled when selected.size === 0; label shows count) + "Abbrechen" ghost.
   - Submit → `startTransition(async () => { const r = await addDocumentsToCaseAction({ caseId, documentIds: [...selected] }); ... })`.
   - On ok:true → toast "Dokument hinzugefügt." (single) or "{n} Dokumente hinzugefügt." (multiple); close sheet; `router.refresh()`.
   - On error 'DOC_ALREADY_ASSIGNED' → toast.error "Dokument ist bereits einem anderen Fall zugeordnet."; refresh to remove stale docs from the picker.

2. `RemoveDocumentDialog.tsx` ("use client"):
   - Props: `{ caseId; caseDocumentId; open; onOpenChange }`.
   - Wraps shadcn `<Dialog>`. Heading "Dokument aus Fall entfernen?", body "Das Dokument bleibt erhalten und kann einem anderen Fall zugeordnet werden.".
   - CTAs: "Abbrechen" (ghost, onClick=onOpenChange(false)) + "Entfernen" (destructive variant) — on confirm, `startTransition(() => removeDocumentFromCaseAction({ caseId, caseDocumentId }))`.
   - On success → toast "Dokument entfernt."; close dialog; `router.refresh()`.
   - Destructive button stays focused initially per accessibility (shadcn default — autofocus on first interactive).

3. `RegenerateDialog.tsx` ("use client"):
   - Props: `{ open; onOpenChange; onConfirm }`.
   - Heading "Laufliste erneut erstellen?", body "Eine neue Laufliste wird erzeugt. Die bisherige bleibt im Verlauf verfügbar.".
   - CTAs "Abbrechen" + "Erneut generieren" (accent, not destructive — per UI-SPEC). onConfirm delegates back to `CaseDetailClient` which triggers `generateLauflisteAction`.
   - Only mounted by CaseDetailClient when `latestLauflisteId !== null` and user clicked the regenerate button.

4. Wire `AddDocumentsSheet` into `CaseDetailClient` (or directly into `page.tsx` using a small client wrapper): open via the "Dokumente hinzufügen" button in the Documents card header + from the empty-state CTA. Pass `assignableDocs` prop from server.

Implements CASE-02 (picker), D-04 (filter reviewed+unassigned), D-19 (copy verbatim), D-20 (Server Actions carry their own auth).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&amp;1 | grep -E "cases/\\[id\\]/(AddDocuments|RemoveDocument|Regenerate)" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>Sheet + 2 dialogs render with exact German copy; sheet multi-select updates footer count; sheet submit inserts case_document rows and refreshes; remove/regenerate confirmation flows wire to their actions; cross-case duplicate toasts surface with correct copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Component page.tsx | caseId crosses in URL |
| Client component → Server Action | personName, documentIds, direction, caseId cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | S (Spoofing) | /cases/[id] server component | mitigate | getCaseForUser wraps ownership; returns null → notFound() (not 403 — prevents case-id enumeration) |
| T-04-20 | T (Tampering) | caseId URL param | mitigate | Not trusted; all queries scope by userId; Server Actions re-verify |
| T-04-21 | I (Info Disclosure) | DocumentsTable server render | mitigate | listCaseDocuments already scopes by owner; no raw user input rendered without escape (React JSX auto-escapes) |
| T-04-22 | T (Tampering) | personName rendered in h1 + breadcrumb | mitigate | React auto-escapes; no dangerouslySetInnerHTML anywhere |
| T-04-23 | T (Tampering) | form CSRF | mitigate | Next 16 Server Actions include CSRF token automatically (framework-provided) |
</threat_model>

<verification>
- `npm run dev` and manually visit `/cases`, `/cases/new`, `/cases/[id]` — smoke-check no runtime errors.
- `npx tsc --noEmit` reports zero new errors in app/(app)/cases/.
- Grep confirms every client component that calls a Server Action is wrapped in `useTransition` or a form action.
- All German copy matches UI-SPEC verbatim (spot-check "Fall anlegen", "Laufliste generieren", "Dokumente hinzufügen", "Erneut generieren", "Nach oben", "Erstellt am").
</verification>

<success_criteria>
- /cases list renders + empty state correct.
- /cases/new creates a case → redirects → sees case detail.
- /cases/[id] shows person header, documents table (add/remove/reorder), Laufliste card, Historie placeholder (Plan 06 populates fully).
- Generate button produces a Laufliste; Historie renders after refresh.
- Blocker banners correct for empty/unreviewed states; CTA disabled.
- Remove + Regenerate dialogs render and confirm correctly.
- Picker sheet multi-selects and inserts rows; cross-case duplicate surfaces correct toast.
</success_criteria>

<output>
After completion, create `.planning/phases/04-laufliste-generation-cases/04-05-SUMMARY.md` per summary template.
</output>
