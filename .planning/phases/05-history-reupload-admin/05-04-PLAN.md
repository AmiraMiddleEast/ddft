---
plan: "05-04"
phase: 5
wave: 4
depends_on: ["05-01"]
requirements: [ADMN-01, ADMN-02, ADMN-03]
files_modified:
  - lib/admin/queries.ts
  - lib/admin/actions.ts
  - lib/admin/actions.test.ts
  - lib/validations/admin.ts
  - app/(app)/admin/behoerden/page.tsx
  - app/(app)/admin/behoerden/authorities/page.tsx
  - app/(app)/admin/behoerden/authorities/[id]/edit/page.tsx
  - app/(app)/admin/behoerden/authorities/[id]/edit/EditAuthorityForm.tsx
  - app/(app)/admin/behoerden/document-types/page.tsx
  - app/(app)/admin/behoerden/document-types/DocumentTypesClient.tsx
  - app/(app)/layout.tsx
autonomous: true
---

# Plan 05-04 — Behörden Admin UI (ADMN-01/02/03)

<objective>
Enable maintenance of Behörden DB: view/search/edit authorities (ADMN-01/02), add/edit document types (ADMN-03). Any logged-in user has admin access (single-user internal tool per CLAUDE.md). No deletion in v1.
</objective>

<must_haves>
- /admin/behoerden dashboard with counts (states, doc types, authorities, needs_review)
- /admin/behoerden/authorities filterable list (state, doc type, needs_review, search)
- /admin/behoerden/authorities/[id]/edit form for authority fields
- /admin/behoerden/document-types list + add new + edit display name
- Zod-validated Server Actions
- Nav link to Behörden admin
- Tests pass
</must_haves>

<task id="05-04-01" type="auto">
<action>
Create:
- `lib/admin/queries.ts` — getAdminStats(), listAuthoritiesAdmin({stateId?, docTypeId?, needsReview?, search?, page?, pageSize?=20}), getAuthorityByIdAdmin(id), listDocumentTypesAdmin(), getDocumentTypeById(id)
- `lib/admin/actions.ts` — updateAuthorityAction(id, patch), createDocumentTypeAction(displayName), updateDocumentTypeAction(id, displayName). All check auth via getSession and return discriminated union.
- `lib/validations/admin.ts` — Zod schemas: authorityPatchSchema (optional fields), documentTypeSchema {displayName: z.string().min(1).max(100)}
- `lib/admin/actions.test.ts` — 4 cases: update authority happy path, create doc type with dedupe, auth required, update doc type preserves slug

Commit: `feat(05-04): add admin queries + actions + Zod schemas`.
</action>

<read_first>
- lib/behoerden/queries.ts
- lib/review/actions.ts (Server Action pattern)
- lib/validations/review.ts (Zod pattern)
- db/schema.ts
</read_first>

<acceptance_criteria>
- `grep "updateAuthorityAction" lib/admin/actions.ts` matches
- 4+ tests in lib/admin/actions.test.ts
- `npx vitest run lib/admin --no-file-parallelism` passes
- `npx tsc --noEmit` clean
</acceptance_criteria>
</task>

<task id="05-04-02" type="auto">
<action>
Create admin pages:
- `app/(app)/admin/behoerden/page.tsx` — Server Component dashboard: 4 stat cards (states, doc types, authorities, needs_review) with links to sub-pages
- `app/(app)/admin/behoerden/authorities/page.tsx` — filterable table; uses nuqs for URL state (search, stateId, docTypeId, needsReview, page); renders rows with "Bearbeiten" links
- `app/(app)/admin/behoerden/authorities/[id]/edit/page.tsx` — Server Component loads authority via getAuthorityByIdAdmin; renders EditAuthorityForm
- `app/(app)/admin/behoerden/authorities/[id]/edit/EditAuthorityForm.tsx` — client form using react-hook-form + zodResolver; submits to updateAuthorityAction
- `app/(app)/admin/behoerden/document-types/page.tsx` — list all doc types
- `app/(app)/admin/behoerden/document-types/DocumentTypesClient.tsx` — client with inline edit + add-new form
- Update `app/(app)/layout.tsx` nav: add "Behörden" link

German copy: "Behörden", "Dashboard", "Behörden bearbeiten", "Dokumentenarten", "Neue Dokumentenart", "Speichern", "Zurück".

Commit: `feat(05-04): add Behörden admin UI + nav link`.
</action>

<read_first>
- app/(app)/cases/page.tsx (list pattern)
- app/(app)/documents/[id]/review/_components/ReviewForm.tsx (form pattern)
- app/(app)/layout.tsx
- components/ui/table.tsx
- components/ui/form.tsx
- components/ui/input.tsx
- components/ui/textarea.tsx
- components/ui/select.tsx
- components/ui/checkbox.tsx
</read_first>

<acceptance_criteria>
- All 6 admin pages exist
- `grep "Behörden" app/\\(app\\)/layout.tsx` matches the nav link
- `npx tsc --noEmit` clean
</acceptance_criteria>
</task>
