---
plan: "05-05"
phase: 5
wave: 5
depends_on: ["05-02", "05-03", "05-04"]
requirements: [HIST-01, HIST-02, HIST-03, UPLD-03, ADMN-01, ADMN-02, ADMN-03]
files_modified:
  - __tests__/phase5-integration.test.ts
autonomous: false
---

# Plan 05-05 — Phase 5 Integration Tests + Human Verify

<objective>
End-to-end integration test for Phase 5 features. Human-verify checkpoint for real-browser UAT. Final plan of v1.0.
</objective>

<must_haves>
- Integration test covers: history query with filters, re-upload versioning, admin authority update
- Human-verify checkpoint returned with ~8 UAT items
</must_haves>

<task id="05-05-01" type="auto">
<action>
Create `__tests__/phase5-integration.test.ts` with integration tests:
1. History: seed user + 3 cases + 3 lauflisten; query with search → matches; query with date range → filters correctly
2. Re-upload: seed user + doc; replace with new file → document.version=2, document_version row has v1 data, storagePath updated
3. Admin: seed authority; call updateAuthorityAction with patch → authority fields updated in DB
4. Cross-user safety: user B cannot updateAuthorityAction on data from user A's context (authorities are shared since no ownership model, so this just confirms auth is required)

Use the existing createTestDb fixture pattern. Mock Anthropic/etc as needed.

Commit: `test(05-05): add phase 5 integration tests`.
</action>

<read_first>
- __tests__/phase4-integration.test.ts (reference pattern)
- __tests__/_fixtures/test-db.ts
- lib/history/queries.ts
- lib/uploads/replace.ts
- lib/admin/actions.ts
</read_first>

<acceptance_criteria>
- `test -f __tests__/phase5-integration.test.ts` ok
- `BETTER_AUTH_SECRET=testsecret12345678901234567890 npx vitest run __tests__/phase5-integration.test.ts` passes
</acceptance_criteria>
</task>

<task id="05-05-02" type="checkpoint:human-verify">
<action>
Return CHECKPOINT REACHED with an 8-step manual UAT checklist covering:
1. /history list renders with entries
2. /history search filter works
3. /history date range filter works
4. Nav "Historie" + "Behörden" links work
5. Re-upload dialog accepts new PDF, version bumps
6. Re-analyze triggers extraction
7. Admin /admin/behoerden/authorities list + filter + edit form save works
8. Admin /admin/behoerden/document-types add + edit works
</action>

<acceptance_criteria>
- Checkpoint returned (agent stops, returns structured message)
</acceptance_criteria>
</task>
