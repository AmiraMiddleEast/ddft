---
plan: "07-03"
phase: 7
wave: 2
depends_on: ["07-01"]
requirements: []
files_modified:
  - app/(app)/cases/page.tsx
  - app/(app)/cases/new/page.tsx
  - app/(app)/cases/new/CreateCaseForm.tsx
  - app/(app)/cases/[id]/page.tsx
  - app/(app)/cases/[id]/CaseDetailClient.tsx
  - app/(app)/cases/[id]/DocumentsTable.tsx
  - app/(app)/cases/[id]/AddDocumentsSheet.tsx
  - app/(app)/cases/[id]/RemoveDocumentDialog.tsx
  - app/(app)/history/page.tsx
  - app/(app)/history/HistoryFilters.tsx
  - lib/cases/actions.ts
autonomous: true
---

# Plan 07-03 — Cases and history

<objective>
Translate case management (list, create, detail, document assignment) and the
Lauflisten history with its filters.
</objective>

<constraints>
Same as 07-01. Additionally:
- The generated PDF's filename and its `Content-Disposition` header may be German
  (e.g. `Laufzettel-…`). Leave the filename scheme alone — the download route test
  asserts it, and the file is a German document.
- The button that generates the PDF is UI ("Laufzettel erzeugen" → "Generate
  Laufzettel"). Keep the word "Laufzettel" as the product term for the German
  document itself — it is the name of the artefact the authorities receive.
</constraints>

<must_haves>
- Cases list, create form, and case detail read in English
- Add-documents sheet and remove-document dialog read in English
- History list and its search/date filters read in English
- Laufzettel generation/download controls read in English, PDF output untouched
- `npx tsc --noEmit` passes; no NEW test failures
</must_haves>

<task id="07-03-01" type="auto">
<action>
Translate `app/(app)/cases/page.tsx`, `cases/new/page.tsx` and
`cases/new/CreateCaseForm.tsx`, plus any user-facing message strings in
`lib/cases/actions.ts`.

Suggested copy:
- "Fälle" → "Cases", "Neuer Fall" → "New case", "Fall anlegen" → "Create case"
- "Name der Person" → "Person's name", "Notizen" → "Notes"
- "Noch keine Fälle" → "No cases yet"
- "Dokumente" → "Documents", "Erstellt" → "Created", "Aktualisiert" → "Updated"
- "Wird angelegt …" → "Creating…"
</action>

<read_first>
- app/(app)/cases/page.tsx
- app/(app)/cases/new/page.tsx
- app/(app)/cases/new/CreateCaseForm.tsx
- lib/cases/actions.ts
</read_first>

<acceptance_criteria>
- `grep -rE "[äöüß]" "app/(app)/cases/page.tsx" "app/(app)/cases/new/"` returns no matches
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="07-03-02" type="auto">
<action>
Translate the case detail screen: `app/(app)/cases/[id]/page.tsx`,
`CaseDetailClient.tsx`, `DocumentsTable.tsx`, `AddDocumentsSheet.tsx`,
`RemoveDocumentDialog.tsx`.

Suggested copy:
- "Dokumente hinzufügen" → "Add documents", "Entfernen" → "Remove"
- "Laufzettel erzeugen" → "Generate Laufzettel", "Wird erzeugt …" → "Generating…"
- "Herunterladen" → "Download", "Neu erzeugen" → "Regenerate"
- "Mindestens ein Dokument ist noch nicht geprüft." → "At least one document has not been reviewed yet."
- "Wirklich entfernen?" → "Remove this document?", "Abbrechen" → "Cancel"

NOTE: `__tests__/ui/case-detail.test.tsx` asserts the unreviewed-docs banner text.
That test is ALREADY FAILING before this phase. Update its expected string to the
new English copy as part of this task, but do not attempt to fix the unrelated
pre-existing failure cause — record it in the plan summary instead.
</action>

<read_first>
- app/(app)/cases/[id]/page.tsx
- app/(app)/cases/[id]/CaseDetailClient.tsx
- app/(app)/cases/[id]/DocumentsTable.tsx
- app/(app)/cases/[id]/AddDocumentsSheet.tsx
- app/(app)/cases/[id]/RemoveDocumentDialog.tsx
- __tests__/ui/case-detail.test.tsx
</read_first>

<acceptance_criteria>
- `grep -rE "[äöüß]" "app/(app)/cases/[id]/"` returns no matches
- `__tests__/ui/case-detail.test.tsx` asserts the English banner string
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="07-03-03" type="auto">
<action>
Translate `app/(app)/history/page.tsx` and `app/(app)/history/HistoryFilters.tsx`.

Suggested copy:
- "Historie" → "History", "Suche" → "Search", "Von" → "From", "Bis" → "To"
- "Name der Person" → "Person's name"
- "Keine Lauflisten gefunden" → "No Lauflisten found"
- "Zum Fall" → "Open case", "Herunterladen" → "Download"
- "Erzeugt am" → "Generated", "Dokumente" → "Documents"

Keep the nuqs query-parameter KEYS unchanged (`search`, `from`, `to`, …) — changing
them would break existing bookmarked URLs and the `shallow: false` wiring.
</action>

<read_first>
- app/(app)/history/page.tsx
- app/(app)/history/HistoryFilters.tsx
</read_first>

<acceptance_criteria>
- `grep -rE "[äöüß]" "app/(app)/history/"` returns no matches
- The set of `useQueryState("…")` keys in `HistoryFilters.tsx` is unchanged
- `npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<verification>
1. `npx tsc --noEmit` exits 0; `npm run build` exits 0
2. Manual: create a case, add a document, generate a Laufzettel, download it.
   UI English; the downloaded PDF is unchanged and German.
</verification>
